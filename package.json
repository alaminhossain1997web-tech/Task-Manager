{
  "name": "task-manager-root",
  "private": true,
  "scripts": {
    "start": "npm --prefix Server start",
    "server": "npm --prefix Server start",
    "client": "npm --prefix Clinte run dev"
  }
}
